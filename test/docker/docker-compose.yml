version: "3.9"

services:
        eneo:
                image: eneo
                volumes:
                        - ../../:/home/bjarne/code/eneo
                          #command: /eneo/rel/eneo/bin/eneo foreground
                command: tail -f /dev/null
        db:
                image: postgres
                restart: always
                environment:
                        POSTGRES_PASSWORD: password
                        POSTGRES_USER: eneo
